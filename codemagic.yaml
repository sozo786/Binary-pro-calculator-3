workflows:
  android:
    name: Android Build
    environment:
      java: 11
    scripts:
      - name: Build with Gradle
        script: |
          gradle wrapper
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk
